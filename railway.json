{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "cd backend/HirelyBackend && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend/HirelyBackend && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn HirelyBackend.wsgi --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/admin/"
  }
}